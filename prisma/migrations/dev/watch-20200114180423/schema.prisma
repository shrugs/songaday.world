datasource db {
  provider = "postgresql"
  url = "***"
}

generator photon {
  provider = "photonjs"
}

model MagicCode {
  code      String   @id
  user      User
  createdAt DateTime @default(now())
}

model User {
  id          String     @default(cuid()) @id
  email       String     @unique
  displayName String?
  magicCode   MagicCode?
}
