# Migration `20200115182949`

This migration has been generated by Matt Condon (shrugs) at 1/15/2020, 6:29:50 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "songadayworld"."Song" (
    "beard" text  NOT NULL DEFAULT 'Beard',
    "id" text  NOT NULL ,
    "instrument" text  NOT NULL DEFAULT 'Organ',
    "location" text  NOT NULL DEFAULT 'Vermont',
    "mood" text  NOT NULL DEFAULT 'Angry',
    "number" integer  NOT NULL DEFAULT 0,
    "owner" text   ,
    "topic" text  NOT NULL DEFAULT 'Kids',
    PRIMARY KEY ("id")
) 

CREATE UNIQUE INDEX "Song.number" ON "songadayworld"."Song"("number")

CREATE UNIQUE INDEX "Song_owner" ON "songadayworld"."Song"("owner")

ALTER TABLE "songadayworld"."Song" ADD FOREIGN KEY ("owner") REFERENCES "songadayworld"."User"("id") ON DELETE SET NULL
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200115122102-add-display-name..20200115182949
--- datamodel.dml
+++ datamodel.dml
@@ -1,13 +1,45 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("POSTGRES_URL")
 }
 generator photon {
   provider = "prisma-client-js"
 }
+enum Location {
+  Vermont
+}
+
+enum Topic {
+  Kids
+}
+
+enum Mood {
+  Angry
+}
+
+enum Beard {
+  Beard
+  None
+}
+
+enum Instrument {
+  Organ
+}
+
+model Song {
+  id         String     @default(cuid()) @id
+  number     Int        @unique
+  owner      User?
+  location   Location
+  topic      Topic
+  mood       Mood
+  beard      Beard
+  instrument Instrument
+}
+
 model MagicCode {
   code      String   @id
   user      User
   createdAt DateTime @default(now())
@@ -17,5 +49,6 @@
   id          String     @default(cuid()) @id
   email       String     @unique
   displayName String?
   magicCode   MagicCode?
+  song        Song?
 }
```


